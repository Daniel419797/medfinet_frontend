import { useCallback, useContext, useEffect, useState } from 'react';
import { Blocks, CheckCircle2, RefreshCw, ShieldX } from 'lucide-react';
import UserContext from '../../contexts/UserContext';
import { PageFeedback } from '../../components/common/PageFeedback';
import { AnchorReceipt, medfinetBlockchainApi } from '../../services/medfinetBlockchainApi';
const button = 'rounded-lg border bg-white px-3 py-2 text-sm font-semibold disabled:opacity-50';
export default function BlockchainEvidence() { const { organizationId } = useContext(UserContext); const [anchors, setAnchors] = useState<AnchorReceipt[]>([]); const [health, setHealth] = useState<Awaited<ReturnType<typeof medfinetBlockchainApi.health>> | null>(null); const [verified, setVerified] = useState<Record<string, boolean>>({}); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const load = useCallback(async () => { if (!organizationId) return; setLoading(true); setError(null); try { const [rows, state] = await Promise.all([medfinetBlockchainApi.list(organizationId), medfinetBlockchainApi.health(organizationId)]); setAnchors(rows); setHealth(state); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to load anchor evidence'); } finally { setLoading(false); } }, [organizationId]); useEffect(() => { void load(); }, [load]); const verify = async (item: AnchorReceipt) => { if (!organizationId) return; try { const result = await medfinetBlockchainApi.verify(organizationId, item.anchorId); setVerified((all) => ({ ...all, [item.anchorId]: result.hashIntegrity })); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to verify anchor'); } }; return <main className="space-y-6"><div className="flex justify-between gap-4"><div><p className="text-sm font-semibold text-cyan-700">Tamper-evident evidence</p><h1 className="text-3xl font-bold">Blockchain anchors</h1><p className="mt-2 text-sm text-slate-600">Audit receipts and deterministic hash-integrity verification.</p></div><button className={button} onClick={() => void load()}><RefreshCw className="mr-2 inline h-4 w-4" />Refresh</button></div>{health && <div className={`rounded-xl border p-4 text-sm ${health.enabled && health.reachable ? 'border-emerald-200 bg-emerald-50 text-emerald-900' : 'border-amber-200 bg-amber-50 text-amber-900'}`}><Blocks className="mr-2 inline h-5 w-5" />{health.enabled ? `${health.network} · ${health.reachable ? 'reachable' : 'unreachable'}` : 'Blockchain anchoring is disabled in this environment'}</div>}<PageFeedback loading={loading} error={error} empty={!anchors.length} onRetry={() => void load()}><div className="overflow-x-auto rounded-xl border bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-3">Anchor</th><th className="p-3">Category</th><th className="p-3">Status</th><th className="p-3">Transaction</th><th className="p-3">Integrity</th></tr></thead><tbody>{anchors.map((item) => <tr key={item.anchorId} className="border-t"><td className="max-w-xs break-all p-3 font-mono text-xs">{item.anchorId}</td><td className="p-3">{item.eventCategory || item.eventCode}</td><td className="p-3">{item.status}</td><td className="max-w-xs break-all p-3 text-xs">{item.txId || 'Pending'}</td><td className="p-3">{item.anchorId in verified ? verified[item.anchorId] ? <span className="text-emerald-700"><CheckCircle2 className="mr-1 inline h-4 w-4" />Valid</span> : <span className="text-red-700"><ShieldX className="mr-1 inline h-4 w-4" />Mismatch</span> : <button className={button} onClick={() => void verify(item)}>Verify</button>}</td></tr>)}</tbody></table></div></PageFeedback></main>; }
